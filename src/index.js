import React from 'react';
import ReactDOM from 'react-dom';

const element = <h1>Hello World</h1>;
//console.log(element);
ReactDOM.render(element, document.getElementById('bruh')); //rendering the element to the DOM - Should be 'root' lol but i made it 'bruh'
